git init
git remote add origin 주소

git에서는 pull이 push보다 선행
git pull origin master(지정된 repository에서 해당 폴더로 끌어오는 커맨드)
git status (현재 git과 싱크상태 체크)
git add . (변경된 파일 모두 트랙킹)
git commit -m "message" (변경사항 commit 달 수 있는 명령어)
git push origin +master

